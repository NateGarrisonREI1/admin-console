export default async function DebugCalendarPage() {
  return (
    <pre style={{ padding: 24 }}>
      Google Calendar integration is temporarily disabled.
      {"\n"}
      UI development may continue safely.
    </pre>
  );
}
